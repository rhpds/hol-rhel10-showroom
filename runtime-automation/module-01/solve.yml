---
# Module 01 — Solve (stub)
# TODO: implement solve tasks for this module.

- name: Solve Module 01
  hosts: bastion
  gather_facts: false
  become: true
  vars:
    ansible_user: "{{ bastion_user | default('lab-user') }}"
  tasks:
    - name: Stub — no solve tasks yet
      ansible.builtin.debug:
        msg: "Solve stub — implement tasks here"
