---
# Module 01 — Validate (stub)
# TODO: implement validation checks for this module.

- name: Validate Module 01
  hosts: bastion
  gather_facts: false
  become: true
  vars:
    ansible_user: "{{ bastion_user | default('lab-user') }}"
  tasks:
    - name: Stub check
      validation_check:
        check: true
        pass_msg: "✅ Module 01 — stub validation passed"
        error_msg: "❌ Module 01 — stub validation failed"
