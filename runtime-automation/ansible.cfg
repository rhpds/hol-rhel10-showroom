[defaults]
inventory = /app/runtime-automation/inventory
host_key_checking = False
remote_user = lab-user
private_key_file = /app/.ssh/id_rsa
forks = 5
become_method = sudo

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ConnectTimeout=30
pipelining = False
